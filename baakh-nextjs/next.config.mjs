/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure App Router is used (this is the default in Next.js 13+)
  // Remove deprecated experimental.appDir option
};

export default nextConfig;
