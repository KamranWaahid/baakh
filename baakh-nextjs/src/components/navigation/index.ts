// Main Navigation Components
export { default as MainNavigation } from '../ui/MainNavigation';
export { default as NavigationProvider } from './NavigationProvider';
export { default as PageNavigation } from '../ui/PageNavigation';
export { default as SearchOverlay } from '../ui/SearchOverlay';

// Legacy Navigation (keeping for backward compatibility)
export { default as Navigation } from '../ui/Navigation';
